# Agent Notes

Use `$jeonse-risk-reporter` or the skill name `jeonse-risk-reporter` when the user asks for Korean 전세/월세 매물 분석, 전세사기 위험 평가, or a one-page rental risk report from structured YAML and mock CSV data.

Do not fetch external listing, registry, or market data. Use only files supplied in the workspace and mark missing information as `⚫ 확인 불가`.
